#include<iostream>
#include<bits/stdc++.h>

using namespace std;

int callRecursion(int n) {
    if(n<= 0) {
        return 0;
    }

    // int i = random(n-1);

    // return callRecursion(i) + callRecursion(n-1-i);
}

int main()
{
    // callRecursion(5);
    int ans = -12/10;
    cout<<ans;
    return 0;
}