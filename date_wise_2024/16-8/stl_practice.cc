#include<iostream>
#include<set>

using namespace std;

int main() {
    /* UPPER BOUND - LOWER BOUND */
    // set<int> st;

    // st.insert(10);
    // st.insert(20);
    // st.insert(30);
    // st.insert(40);
    // st.insert(50);
    // st.insert(60);

    // cout<<*(st.lower_bound(46))<<endl;
    // cout<<*(st.upper_bound(46))<<endl;

    /* SET */

    // set<int> st;
    
    // st.insert(10);
    // st.insert(20);
    // st.insert(30);
    // st.insert(40);
    // st.insert(50);
    // st.insert(60);

    // cout<<*(st.find(40));

    /* decending set */

    // set<int,greater<int>> st;
    
    // st.insert(10);
    // st.insert(20);
    // st.insert(30);
    // st.insert(40);
    // st.insert(50);
    // st.insert(60);

    // set<int,greater<int>> :: iterator it;

    // for(it = st.begin();it!= st.end(); ++it) {
    //     cout<<*it<<" ";
    // }

    
    return 0;
}